# FlightMate 1.08 构建与验证记录

- 构建日期：2026-08-01
- 独立构建：通过
- 目标：ESP32-S3 / LilyGo T-LoRa Pager V1.0
- App 代码占用：1,668,969 字节，App 分区占用 53%
- 全局变量：65,080 字节，内部动态内存占用 19%
- App 镜像：1,669,216 字节
- 合并镜像：16,777,216 字节

## 飞行页更新检查

- App 版本号已更新为 `1.08`。
- 地图宽度调整为 300 px，右侧数据栏宽度为 180 px。
- 数据栏显示 GNSS 状态、UTC、速度、高度、航向角、纬度、经度、卫星数、HDOP 和地图状态。
- 速度、高度和航向使用 2 倍字体；经纬度、卫星/HDOP 和地图状态位于右侧栏内。
- 左侧地图瓦片裁剪范围同步使用 300 px 宽度，当前位置标记仍以地图区域中心为基准。

## METAR 问题检查

- 更正 4 个旧/错误 ICAO：`ZULS`、`ZSQZ`、`ZJHK`、`ZHCC`。
- 以更正后的 54 个机场查询 AviationWeather 最近 2 小时数据：48 个返回 METAR，6 个返回 HTTP 204。
- HTTP 204 表示数据源没有该时段报文，1.07 会回查 24 小时；仍为空则显示 `NO REPORT` 并等待正常刷新周期，不再每分钟重复请求。
- TLS 启动失败、负值网络错误和正值 HTTP 错误分别显示，并记录 ICAO、时间窗口、HTTP 状态、响应长度和解析长度。

## UI 静态边界

- 右面板范围：x=166～466；正文范围：x=178～454。
- 2 倍字体正文 6 行，最后一行底部 y=166；更新时间底部 y=178；面板底部 y=180。
- 左侧 UTC 文本底部 y=167；未超出机场卡片。
- 飞行页地图范围：x=0～299，数据栏范围：x=300～479；所有飞行数据文本位于数据栏内。

## 校验结果

- Wi-Fi 密码编辑调用不再启用 secret 模式，设置列表仍使用星号：通过。
- 1.06 地图 PNG 修复、TrailMate 路径、1～9 级缩放和单条 METAR 行为保留：通过。
- 1.08 飞行页数据栏静态布局：通过。
- 源码无 NUL 字节：通过。
- ESP32-S3 App 镜像 checksum 与 validation hash：有效。
- 16 MB 合并镜像 bootloader checksum 与 validation hash：有效。
- 发布固件与独立构建产物 SHA-256 一致：通过。

## 构建警告

Arduino-ESP32 预编译 newlib 对象产生 `.note.GNU-stack` 弃用警告；未影响链接和镜像生成。

## 真机验证边界

当前工作环境未连接设备。实际字体可读性、UTC 时间、Wi-Fi 密码输入、GNSS 高度/速度/航向、地图瓦片显示和不同机场的实时数据状态仍需烧录 1.08 后真机确认。
