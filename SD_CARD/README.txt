FlightMate 1.07 可直接使用 SD 卡中已有的 TrailMate 地图瓦片，无需移动文件。

TrailMate 目录：
  /maps/base/osm/{z}/{x}/{y}.png

仍兼容 FlightMate 目录：
  /FlightMate/maps/base/osm/{z}/{x}/{y}.png

要求：256×256 PNG、Web Mercator、XYZ、缩放 1～9。

manifest.json 可选；缺少时固件会进入 Direct tiles 模式。
本发布包不包含实际地图瓦片。
