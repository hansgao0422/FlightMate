# **FlightMate计划**

## 一、适配硬件

设备：LilyGo T-LoRa Pager
硬件参数：ESP32-S3、16MB Flash、8MB PSRAM、ST7796U 显示屏，分辨率480×222

## 二、功能设计

### 1. 机场面板（需要Wi-Fi联网）
1. 时间显示：本地时间、UTC时间、日期同步展示
2. 机场匹配：支持GPS定位/手动输入经纬度，自动匹配附近机场并显示ICAO 4位机场代码
3. 气象报文：读取对应机场本场METAR气象短报文
4. 数据缓存机制
   - 联网刷新后缓存机场、气象数据，断网可正常查看
   - 无手动刷新操作时，每30分钟自动联网更新一次

### 2. 飞行面板（必须开启GPS）
1. 地图可视化
   - 读取SD卡内瓦片地图，本机位置默认在画面中心，飞机图标标记点位
   - 地图缩放等级范围：5~9级
2. 实时数据展示：GPS实时速度、航向、高度、当前经纬度
3. 前置校验：未开启GPS进入面板会弹窗提示开启GPS开关
4. GPS后台逻辑：GPS开启后后台持续保持连接状态
5. 地图快捷键控制
   - Q / E：放大、缩小地图等级
   - W / S / A / D：上下左右平移地图画面
   - C：一键将自身定位重置至地图画面中心

### 3. 飞行日志面板（依赖SD存储卡）
#### 日志完整记录字段
1. 航班基础信息：乘机日期、起飞/到达当地时间、航班号、起降机场航线
2. 航空器信息：航空公司、机型、注册号、发动机型号、机龄
3. 座位舱位信息：舱位等级、座位号
4. 飞行核心参数：起飞跑道、落地跑道、巡航高度、飞行时长、实际飞行距离、巡航速度
5. 地面&乘机体验：登机口、推出/起飞/落地/滑入机位时间、远机位/廊桥区分、餐食与机上服务评价

#### 日志操作模式
1. 功能入口：新建日志（编辑模式）、历史日志列表
2. 浏览/编辑切换：打开已有日志默认只读浏览模式，按下E键切换编辑模式
3. 录入方式：下拉选项选择、数字/字母简易输入
4. 排序规则：日志列表按乘机日期时间排序

#### SD卡存储规则
1. 全新SD卡插入后，进入日志面板自动校验，无`FlightLog`文件夹则自动创建
2. 日志文件命名规则：日期+时间+航班号
3. SD卡同时存储地图瓦片资源

### 4. 设置面板
1. Wi-Fi设置：扫描周边无线网络、填写密码连接
2. 时区配置：手动自定义本地时区
3. 屏幕亮度：4档可调（25% / 50% / 75% / 100%）
4. 自动熄屏时长：30s、60s、5分钟、10分钟、常亮
5. 手动经纬度录入：无GPS信号时替代定位，用于拉取机场、气象、校准时间
6. GPS总开关：控制GPS硬件启停，为机场面板、飞行面板提供定位数据源

### 5. 全局通用附加功能
1. 系统基础功能：实时电量显示、整机开关机
2. L键（屏幕亮度调节）
   - 生效范围：机场面板、飞行面板
   - 功能：循环切换4档屏幕亮度，档位自动保存记忆
3. B键（键盘背光调节）
   - 生效范围：全部功能面板通用
   - 档位：熄灭、中等亮度、最亮三档循环切换，档位自动保存

## 三、全局统一操作逻辑
1. 主界面交互：首页展示各功能面板图标，滚轮选择面板，确认键进入对应页面
2. 返回逻辑：任意面板非文字输入状态下，按下返回键直接退回主界面
3. 存储介质分工：SD卡统一存放飞行日志文件、地图瓦片资源

## 四、UI设计规范
1. 整体视觉风格参考FlightRadar，全设备界面视觉体系统一
2. 交付要求：输出各功能面板效果图，作为界面开发参考
